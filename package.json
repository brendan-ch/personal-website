{
  "name": "personal-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run generate && next dev",
    "build": "npm run generate && next build",
    "generate": "node scripts/generateImages.js",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc",
    "test": "npm run generate && jest --watch",
    "test:ci": "npm run generate && jest --ci",
    "test:coverage": "npm run generate && jest --ci --coverage"
  },
  "dependencies": {
    "@vercel/analytics": "^0.1.8",
    "next": "^14.1.0",
    "plaiceholder": "^3.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sharp": "^0.30.7"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^14.0.0",
    "@testing-library/user-event": "^14.4.3",
    "@types/node": "17.0.36",
    "@types/nodemailer": "^6.4.5",
    "@types/react": "^18.2.55",
    "@types/react-dom": "^18.2.19",
    "@vercel/nft": "^0.21.0",
    "@vercel/node": "^2.5.8",
    "eslint": "8.16.0",
    "eslint-config-next": "^14.1.0",
    "jest": "27.5.1",
    "typescript": "4.7.2"
  }
}
